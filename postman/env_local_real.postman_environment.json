{
  "id": "env-local-real",
  "name": "NetShield — Local Real",
  "values": [
    { "key": "base_url", "value": "http://localhost:8000", "enabled": true },
    { "key": "ws_url",   "value": "ws://localhost:8000",   "enabled": true }
  ],
  "_postman_variable_scope": "environment",
  "info": {
    "description": "Servidor local contra servicios reales del laboratorio.\nRequiere MikroTik CHR, Wazuh, GLPI y Anthropic alcanzables.\nIniciar con: cd backend && uvicorn main:app --reload\n(Las variables MOCK_* deben estar ausentes o en false en .env)"
  }
}
