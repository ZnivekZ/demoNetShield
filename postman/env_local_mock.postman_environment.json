{
  "id": "env-local-mock",
  "name": "NetShield — Local Mock",
  "values": [
    { "key": "base_url", "value": "http://localhost:8000", "enabled": true },
    { "key": "ws_url",   "value": "ws://localhost:8000",   "enabled": true }
  ],
  "_postman_variable_scope": "environment",
  "info": {
    "description": "Servidor local con MOCK_ALL=true.\nNo requiere MikroTik, Wazuh, GLPI ni Anthropic.\nIniciar con: cd backend && MOCK_ALL=true uvicorn main:app --reload"
  }
}
